---
title: Quick Start
description: Getting Started with WebGL Fluid Enhanced
icon: Play
---

## Introduction

I wanted to incorporate Pavel Dobryakov's [WebGL Fluid Simulation](https://github.com/PavelDoGreat/WebGL-Fluid-Simulation) into my [personal website](https://www.michaelbrusegard.com), but the original project did not provide the tools for it. Hence, I decided to enhance the original project by:

- Introducing new cool features
- Adding ES Module support
- Providing a minified build for optimized performance
- Eliminating unnecessary boilerplate UI
- Restructuring the code into classes and enforcing strict TypeScript rules for an improved developer experience

## Installation

Scaffold a website with your chosen framework (Next.js, Angular, Vue, etc.).

Install the package from the npm registry using your package manager of choice.

<Tabs id='package-manager' persist items={['npm', 'pnpm', 'yarn', 'bun']}>

```bash tab="npm"
npm install webgl-fluid-enhanced
```

```bash tab="pnpm"
pnpm install webgl-fluid-enhanced
```

```bash tab="yarn"
yarn add webgl-fluid-enhanced
```

```bash tab="bun"
bun add webgl-fluid-enhanced
```

</Tabs>

For a simple HTML website you can load the script [directly](/docs/frameworks#html).

## Initialization

To initialize the WebGL Fluid Simulation, import the `WebGLFluidEnhanced` class and create a new instance.

```javascript
import WebGLFluidEnhanced from 'webgl-fluid-enhanced';

const simulation = new WebGLFluidEnhanced();
```

You can pass an optional container element to the constructor to render the simulation inside it:

```javascript
const container = document.querySelector('div');

const simulation = new WebGLFluidEnhanced(container);
```

If not provided, the simulation will be rendered in the body element.

## Start & Stop

To start the simulation, call the `start` method on the instance.

```javascript
simulation.start();
```

And to stop the simulation, call the `stop` method.

```javascript
simulation.stop();
```

<Callout type='warn'>
  Call `stop` before removing the simulation from the DOM to avoid memory leaks.
</Callout>

## FAQ

Some common questions you may encounter.

<Accordions>
  <Accordion id='which-new-features' title="Which new features are provided?">
    WebGL Fluid Enhanced provides many new features outside of Typescript and ES Module support. Some of the new features include:

      - Changing configuration options on the fly
      - Trigger splats
      - Splat a specific location
      - Using hover to activate
      - Brightness option
      - Pause/resume simulation
      - Custom color palette
      - Invert colors option
      - Set custom background image/color

  </Accordion>
</Accordions>
